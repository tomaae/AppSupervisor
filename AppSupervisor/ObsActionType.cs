namespace AppSupervisor;

/// <summary>Identifies the deterministic OBS operation issued when a profile activates.</summary>
public enum ObsActionType
{
    SwitchScene,
    SetInputMute,
    SetSourceVisibility
}
